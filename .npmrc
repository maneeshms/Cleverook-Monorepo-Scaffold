# Enforce the scaffold's exact-pin policy: `pnpm add <pkg>` writes an exact
# version (never a ^/~ range). See docs/agents/conventions.md. Applies to the root
# workspace; each standalone client app repeats it locally, because their Docker
# builds copy only their own directory and never see this file.
save-exact=true
