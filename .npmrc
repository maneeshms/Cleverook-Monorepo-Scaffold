# Enforce the scaffold's exact-pin policy: `npm install <pkg>` writes an exact
# version (never a ^/~ range). See docs/agents/conventions.md. Applies to the root
# workspace and, since npm walks up for .npmrc, to the standalone frontends too.
save-exact=true
