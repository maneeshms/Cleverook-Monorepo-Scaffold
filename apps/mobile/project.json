{
  "name": "mobile",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/mobile",
  "projectType": "application",
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "[ -d node_modules ] || pnpm install --frozen-lockfile; pnpm exec expo start",
        "cwd": "apps/mobile"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "[ -d node_modules ] || pnpm install --frozen-lockfile; pnpm run typecheck && pnpm run build",
        "cwd": "apps/mobile"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "[ -d node_modules ] || pnpm install --frozen-lockfile; pnpm run lint",
        "cwd": "apps/mobile"
      }
    }
  },
  "tags": ["scope:mobile", "type:app"]
}
