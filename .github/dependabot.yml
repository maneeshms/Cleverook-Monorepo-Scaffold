# Dependabot is intentionally inert on this VALIDATION MIRROR (no version-update
# PRs, so it never spends Actions minutes). The real scaffold keeps the full
# config. Security ALERTS remain on; only automated version PRs are disabled here.
version: 2
updates: []
