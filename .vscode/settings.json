{
  // Use the workspace TypeScript (pinned in package.json) instead of the IDE's
  // bundled one, so editor diagnostics match `npm run typecheck` exactly.
  "typescript.tsdk": "node_modules/typescript/lib",
  "typescript.enablePromptUseWorkspaceTsdk": true
}
